"""
MedBridge AI - MCP Server Module
==================================

This package contains the Model Context Protocol (MCP) server that exposes
healthcare tools (drug interaction lookup, calendar scheduling) over a
standardized protocol. The server runs as a separate subprocess and
communicates via stdio, demonstrating a decoupled tool architecture.

Kaggle Rubric Alignment: MCP Server (Code)
"""
